@* Single tile renderer for the zone tilemap. Renders a <div> with the appropriate CSS class and overlays. *@
@* Note: The Tile type used in @code is Veldrath.GameClient.Core.Services.Tile, not this component. *@

<div class="game-tile @GetTileClass()"
     @onclick="() => OnClick.InvokeAsync((X, Y))"
     title="@($"({X}, {Y})")">
    @if (IsPlayerHere)
    {
        <div class="tile-indicator tile-indicator-player" title="You are here"></div>
    }
    @if (Occupant is not null)
    {
        <div class="tile-indicator tile-indicator-occupant" title="@Occupant.CharacterName"></div>
    }
    @if (Enemy is not null)
    {
        <div class="tile-indicator tile-indicator-enemy" title="@Enemy.EnemyName (Lv. @Enemy.Level)"></div>
    }
</div>

@code {
    /// <summary>The tile data to render.</summary>
    [Parameter] public Veldrath.GameClient.Core.Services.Tile TileData { get; set; } = null!;

    /// <summary>The column position of this tile in the grid.</summary>
    [Parameter] public int X { get; set; }

    /// <summary>The row position of this tile in the grid.</summary>
    [Parameter] public int Y { get; set; }

    /// <summary>Callback when the tile is clicked. Provides the tile coordinates.</summary>
    [Parameter] public EventCallback<(int X, int Y)> OnClick { get; set; }

    /// <summary>Whether the player is currently on this tile.</summary>
    [Parameter] public bool IsPlayerHere { get; set; }

    /// <summary>An occupant (other player) on this tile, if any.</summary>
    [Parameter] public Veldrath.GameClient.Core.Services.OccupantInfo? Occupant { get; set; }

    /// <summary>An enemy on this tile, if any.</summary>
    [Parameter] public Veldrath.GameClient.Core.Services.EnemyInfo? Enemy { get; set; }

    private string GetTileClass() => TileData.Type switch
    {
        0 => "tile-grass",
        1 => "tile-wall",
        2 => "tile-water",
        3 => "tile-door",
        4 => "tile-path",
        5 => "tile-dirt",
        -1 => "tile-void",
        _ => "tile-empty"
    };
}
